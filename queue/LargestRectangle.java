public class LargestRectangle {

    /**
     * between [i, j] max area is min(heights[i], ..., heights[j]) * (j-i+1)
     * 0 -> 1..n-1
     * 1 -> 2..n-1
     * 2 -> 3..n-1
     * -> O(n^2) (monotonic stack)
     */
    public findMaxArea(int[] heights) {}
}
